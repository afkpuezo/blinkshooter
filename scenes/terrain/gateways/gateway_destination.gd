extends Node2D
class_name GatewayDestination
## Add this to a Gateway to control where it sends the player.
# NOTE: I wasn't sure if this should even be its own scene or script, as opposed
# to just using a Node2D. At least this way there could maybe be an animation
# or something
